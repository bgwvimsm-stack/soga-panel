# 这是生产环境的示例环境变量文件
# 复制此文件并命名为 .env.production，然后根据需要修改变量值
# 线上环境平台打包路径
VITE_PUBLIC_PATH = /

# 线上环境路由历史模式（Hash模式传"hash"、HTML5模式传"h5"、Hash模式带base参数传"hash,base参数"、HTML5模式带base参数传"h5,base参数"）
VITE_ROUTER_HISTORY = "hash"

# 是否在打包时使用cdn替换本地库 替换 true 不替换 false
VITE_CDN = false

# 是否启用gzip压缩或brotli压缩（分两种情况，删除原始文件和不删除原始文件）
# 压缩时不删除原始文件的配置：gzip、brotli、both（同时开启 gzip 与 brotli 压缩）、none（不开启压缩，默认）
# 压缩时删除原始文件的配置：gzip-clear、brotli-clear、both-clear（同时开启 gzip 与 brotli 压缩）、none（不开启压缩，默认）
VITE_COMPRESSION = "none"

# API 配置 - 生产环境，使用相对路径通过Service Binding
VITE_API_BASE_URL = /api
VITE_API_TIMEOUT = 10000

# Soga Panel 基础配置
VITE_SITE_NAME = "SogaPanel"
# 是否强制指定站点名称，覆盖后端返回的站点名称 true 强制覆盖 false 不覆盖
VITE_FORCE_SITE_NAME = false
VITE_APP_VERSION = "1.0.0"

# Google OAuth 客户端ID
# 从 Google Cloud Console 获取你的 OAuth 2.0 客户端ID
# 配置后可启用 Google 登录功能
# 留空则不启用 Google 登录功能
# VITE_GOOGLE_CLIENT_ID =

# GitHub OAuth 客户端ID
# 从 GitHub Developer Settings 创建 OAuth App 后获取 Client ID
# 配置后前端可触发 GitHub 登录流程
# VITE_GITHUB_CLIENT_ID =

# Cloudflare Turnstile
# 从 Cloudflare Turnstile 控制台获取你的网站密钥 (Site Key)
# VITE_TURNSTILE_SITE_KEY =

# Crisp客服系统
# 从 https://app.crisp.chat 获取你的网站ID (Website ID)
# 配置后客服系统将自动推送用户详细信息给客服人员
# 留空则不启用客服系统
# VITE_CRISP_WEBSITE_ID =
