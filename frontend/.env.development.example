# Soga Panel 开发环境配置
# 复制此文件并命名为 .env.development，然后根据需要修改变量值
VITE_SITE_NAME = "SogaPanel 开发环境"
# 是否强制指定站点名称，覆盖后端返回的站点名称 true 强制覆盖 false 不覆盖
VITE_FORCE_SITE_NAME = true
# 本地运行端口号
VITE_PORT=8848
# 开发环境相关
VITE_MOCK = false
VITE_DEV_PROXY = true
# 后端接口地址
VITE_BACKEND_URL=http://localhost:18787/api
# API 配置
VITE_API_BASE_URL=/api
VITE_API_TIMEOUT=10000
# 前端与后端通信密钥，需与后端配置一致
VITE_FRONTEND_API_SECRET = "123456"
# 邮箱后缀预设列表（JSON 数组或逗号分隔字符串），留空则可填写任意后缀
# VITE_EMAIL_SUFFIX_OPTIONS = ["gmail.com", "qq.com"]
